let velocity = 260;
let friction = 1;

function calculateMeters(velocity, friction){
    return distance = Math.pow(velocity, 2)/ 250*friction;
}

calculateMeters(velocity, friction);

const result = `A distância necessária para que o veículo seja freado totalmente é de ${distance} metros. `
console.log(result);