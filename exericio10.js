number = 1969;

function calculateRow (number){
    return row = Math.ceil(Math.sqrt(number));
}

calculateRow(number);
const result = `A linha em que o número 1969 se encontra é a linha ${row}.`
console.log(result);